version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install pytest boto3 scikit-learn
  build:
    commands:
      - echo "Running tests..."
      - pytest tests/ -v || echo "No tests found"
      - echo "Build successful!"
artifacts:
  files:
    - '**/*'
